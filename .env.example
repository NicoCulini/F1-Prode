# Copy relevant values into PythonAnywhere's "Web" tab → "Environment variables" section.
# Generate a secret key by running:
#   python -c "from config import generate_secret_key; generate_secret_key()"

SECRET_KEY=replace-this-with-a-long-random-string
